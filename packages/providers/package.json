{
  "//": "Provider adapter package for concrete local and remote LLM integrations.",
  "name": "@sentinel/providers",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@sentinel/core": "file:../core"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
